# Copy this file to .env and set OPENAI_COMPATIBLE_API_KEY locally.
# Never commit the real .env file.
OPENAI_COMPATIBLE_BASE_URL=https://llm-90bgvobhtlb3dukk.cn-beijing.maas.aliyuncs.com/compatible-mode/v1
OPENAI_COMPATIBLE_API_KEY=
OPENAI_COMPATIBLE_MODEL=qwen3.7-max-2026-05-17

# Optional: price per 1,000 input/output tokens in USD.
# Leave blank when the provider does not expose a stable price.
MODEL_INPUT_COST_PER_1K=
MODEL_OUTPUT_COST_PER_1K=
